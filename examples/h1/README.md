# Synthetic H1 comparison fixture

The two CSV files in this directory are deliberately small synthetic fixtures. They exercise one
exact match and one value mismatch; they are not extracts from BPS and must not be cited as
research observations.

Run them with `make h1-example`. Replace them with normalized extracts outside this directory for
the real H2 comparison.
